---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const lang = 'ko';

const newsCount = (await getCollection('news', ({ data }) => data.lang === lang && !data.draft)).length;
const wikiCount = (await getCollection('wiki', ({ data }) => data.lang === lang && !data.draft)).length;
const projectCount = (await getCollection('projects', ({ data }) => data.lang === lang && !data.draft)).length;
---

<BaseLayout>
    <div class="landing">
        <section class="hero">
            <p class="hero-eyebrow">AI Knowledge & Intelligence</p>
            <h1 class="hero-title">AIKI</h1>
            <p class="hero-subtitle">중요한 업데이트를 먼저 보고, 필요한 개념은 위키로 확인한 뒤, 실제 실행 결과는 쇼케이스에서 바로 이어서 봅니다.</p>
        </section>

        <section class="categories" aria-label="AIKI 카테고리">
            <a href="/ko/news/" class="category-card news-card">
                <div class="card-icon">
                    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-2 2zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2"/><path d="M18 14h-8m8-4h-8m5 8h-5"/></svg>
                </div>
                <div class="card-body">
                    <h2>AI 뉴스</h2>
                    <p>중요한 업데이트를 빠르게 읽고, 과장된 문장은 줄인 요약으로 흐름만 먼저 따라갑니다.</p>
                    <span class="card-count">{newsCount}개의 기사</span>
                </div>
                <span class="card-arrow">&rarr;</span>
            </a>

            <a href="/ko/wiki/" class="category-card wiki-card">
                <div class="card-icon">
                    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
                </div>
                <div class="card-body">
                    <h2>AI 위키</h2>
                    <p>개념, 기법, 도구, 모델을 연결해서 정리합니다. 처음 보는 용어와 맥락을 빠르게 따라잡을 수 있습니다.</p>
                    <span class="card-count">{wikiCount}개의 용어</span>
                </div>
                <span class="card-arrow">&rarr;</span>
            </a>

            <a href="/ko/projects/" class="category-card projects-card">
                <div class="card-icon">
                    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/></svg>
                </div>
                <div class="card-body">
                    <h2>AI 쇼케이스</h2>
                    <p>직접 실행한 결과를 먼저 보고, 도입 판단에 필요한 설명과 검증 근거를 바로 이어서 확인합니다.</p>
                    <span class="card-count">{projectCount}개의 쇼케이스</span>
                </div>
                <span class="card-arrow">&rarr;</span>
            </a>
        </section>

        <section class="newsletter-section" aria-label="뉴스레터 사전 등록">
            <div class="newsletter-card">
                <h3>AIKI 뉴스레터, 곧 시작합니다.</h3>
                <p>첫 발송 전에 미리 자리 맡아두세요.</p>
                <button type="button" class="newsletter-btn" id="newsletter-trigger">사전 등록하기</button>
            </div>
        </section>
    </div>
</BaseLayout>

<script is:inline src="https://tally.so/widgets/embed.js"></script>
<script is:inline>
    document.getElementById('newsletter-trigger')?.addEventListener('click', function() {
        if (typeof Tally !== 'undefined') {
            Tally.openPopup('kdR9bZ', {
                width: 400,
                emoji: { text: '\u{1F4EC}', animation: 'wave' },
            });
        } else {
            window.open('https://tally.so/r/kdR9bZ', '_blank');
        }
    });
</script>

<style>
    .landing {
        max-width: 800px;
        margin: 0 auto;
    }

    .hero {
        text-align: center;
        padding: 56px 0 48px;
    }

    .hero-eyebrow {
        margin: 0 0 12px;
        color: var(--color-text-muted);
        font-size: var(--text-sm);
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        animation: fadeUp 0.6s ease-out;
    }

    .hero-title {
        font-size: clamp(3.5rem, 10vw, 6rem);
        font-weight: 850;
        letter-spacing: -0.02em;
        background: linear-gradient(135deg, var(--color-news), var(--color-wiki), var(--color-projects));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 16px;
        line-height: 1;
        animation: fadeUp 0.6s ease-out 0.05s both;
    }

    .hero-subtitle {
        margin: 0;
        font-size: var(--text-xl);
        color: var(--color-text-muted);
        line-height: 1.5;
        animation: fadeUp 0.6s ease-out 0.1s both;
    }

    @keyframes fadeUp {
        from { opacity: 0; transform: translateY(12px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .categories {
        display: grid;
        gap: 16px;
        margin-bottom: 48px;
    }

    .category-card {
        display: grid;
        grid-template-columns: 56px minmax(0, 1fr) 28px;
        align-items: center;
        gap: 18px;
        padding: 24px;
        border: 1px solid var(--color-border);
        border-radius: 12px;
        background: var(--color-surface);
        color: var(--color-text);
        text-decoration: none;
        transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        animation: fadeUp 0.5s ease-out both;
    }

    .category-card:nth-child(1) { animation-delay: 0.15s; }
    .category-card:nth-child(2) { animation-delay: 0.22s; }
    .category-card:nth-child(3) { animation-delay: 0.29s; }

    .category-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
    }

    .news-card { border-left: 3px solid var(--color-news); }
    .news-card:hover { border-color: var(--color-news); }
    .news-card .card-icon { color: var(--color-news); background: color-mix(in srgb, var(--color-news) 12%, transparent); }
    .news-card .card-count { color: var(--color-news); }

    .wiki-card { border-left: 3px solid var(--color-wiki); }
    .wiki-card:hover { border-color: var(--color-wiki); }
    .wiki-card .card-icon { color: var(--color-wiki); background: color-mix(in srgb, var(--color-wiki) 12%, transparent); }
    .wiki-card .card-count { color: var(--color-wiki); }

    .projects-card { border-left: 3px solid var(--color-projects); }
    .projects-card:hover { border-color: var(--color-projects); }
    .projects-card .card-icon { color: var(--color-projects); background: color-mix(in srgb, var(--color-projects) 12%, transparent); }
    .projects-card .card-count { color: var(--color-projects); }

    .card-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 56px;
        height: 56px;
        border-radius: 12px;
        flex-shrink: 0;
    }

    .card-body {
        min-width: 0;
    }

    .card-body h2 {
        margin: 0 0 6px;
        font-size: var(--text-lg);
        font-weight: 750;
    }

    .card-body p {
        margin: 0 0 8px;
        color: var(--color-text-muted);
        font-size: var(--text-sm);
        line-height: 1.55;
    }

    .card-count {
        font-size: var(--text-xs);
        font-weight: 700;
    }

    .card-arrow {
        color: var(--color-text-muted);
        font-size: var(--text-xl);
        transition: transform 0.2s ease;
    }

    .category-card:hover .card-arrow {
        transform: translateX(4px);
    }

    .newsletter-section {
        margin-bottom: 48px;
        animation: fadeUp 0.5s ease-out 0.35s both;
    }

    .newsletter-card {
        text-align: center;
        padding: 32px 24px;
        border: 1px solid var(--color-border);
        border-radius: 12px;
        background: var(--color-surface);
    }

    .newsletter-card h3 {
        margin: 0 0 6px;
        font-size: var(--text-lg);
        font-weight: 750;
    }

    .newsletter-card p {
        margin: 0 0 16px;
        color: var(--color-text-muted);
        font-size: var(--text-sm);
    }

    .newsletter-btn {
        display: inline-flex;
        align-items: center;
        min-height: 40px;
        padding: 0 24px;
        border: 1px solid var(--color-border);
        border-radius: 8px;
        background: var(--color-surface-elevated);
        color: var(--color-text);
        font: inherit;
        font-size: var(--text-sm);
        font-weight: 700;
        cursor: pointer;
        transition: background 0.15s ease, border-color 0.15s ease, transform 0.15s ease;
    }

    .newsletter-btn:hover {
        border-color: var(--color-news);
        background: color-mix(in srgb, var(--color-news) 10%, var(--color-surface));
        transform: translateY(-1px);
    }

    @media (max-width: 640px) {
        .hero { padding: 36px 0 32px; }
        .hero-subtitle { font-size: var(--text-lg); }

        .category-card {
            grid-template-columns: 44px minmax(0, 1fr) 24px;
            gap: 14px;
            padding: 18px;
        }

        .card-icon {
            width: 44px;
            height: 44px;
            border-radius: 10px;
        }

        .card-icon svg {
            width: 22px;
            height: 22px;
        }
    }
</style>
