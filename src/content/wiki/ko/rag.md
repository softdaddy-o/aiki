---
term: rag
title: 'RAG (검색 증강 생성)'
lang: ko
summary: 'LLM이 답변을 생성하기 전에 외부 문서를 검색해서 참고하는 기법. 학습 데이터에 없는 최신 정보도 활용할 수 있게 해준다.'
category: technique
aliases:
    - retrieval-augmented generation
    - 검색 증강 생성
relatedTerms:
    - llm
    - embedding
    - vector-db
mentionCount: 0
draft: false
tags:
    - retrieval
    - generation
    - search
---

## 한 줄 정의

RAG(Retrieval-Augmented Generation)는 LLM이 답변하기 전에 외부 데이터베이스에서 관련 문서를 먼저 찾아보는 기법이다.

## 작동 원리

RAG는 크게 두 단계로 동작한다.

1. **검색(Retrieval)**: 사용자 질문을 벡터로 변환한 뒤, 벡터 데이터베이스에서 의미적으로 가까운 문서 조각들을 가져온다.
2. **생성(Generation)**: 가져온 문서 조각들을 프롬프트에 붙여서 LLM에 넘긴다. LLM은 이 문맥을 참고해서 답변을 만든다.

핵심은 LLM이 자기 파라미터에 저장된 지식만 쓰는 게 아니라, 실시간으로 외부 정보를 참조한다는 점이다. 회사 내부 문서, 최신 뉴스, 기술 문서 같은 걸 LLM 재학습 없이 활용할 수 있다.

## 왜 중요한가

LLM의 가장 큰 약점 두 가지가 있다. 학습 데이터 이후의 정보를 모른다는 것, 그리고 없는 내용을 그럴듯하게 지어낸다는 것(할루시네이션). RAG는 이 두 문제를 동시에 줄여준다.

파인튜닝과 비교하면 비용이 훨씬 적게 든다. 모델을 다시 학습시키는 대신 검색 대상 문서만 업데이트하면 되니까. 그래서 기업용 챗봇이나 사내 지식 검색 시스템에서 사실상 표준 패턴이 됐다.

다만 검색 품질이 나쁘면 답변 품질도 같이 떨어진다. 쓰레기가 들어가면 쓰레기가 나온다는 건 RAG에서도 마찬가지다. 임베딩 모델 선택, 청킹 전략, 리랭킹 같은 세부 설계가 실제 성능을 좌우한다.

## 관련 용어

- [LLM](/wiki/ko/llm) — RAG가 생성 단계에서 사용하는 기반 모델
- [임베딩](/wiki/ko/embedding) — 문서와 질문을 벡터로 변환하는 과정
- [벡터 데이터베이스](/wiki/ko/vector-db) — 임베딩된 문서를 저장하고 유사도 검색을 수행하는 저장소
